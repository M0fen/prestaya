/node_modules
/.next
/out
/build
*.tsbuildinfo
next-env.d.ts
.DS_Store
.env*
!.env*.example

.vercel

# Python bytecode de scripts de empalme
__pycache__/
*.pyc
backups-demo/

# Log reversible de consolidación de asignaciones (artefacto local)
scripts/_consolidacion_revert_*.json
scripts/_import_zc_revert_*.json

# Exports de Disapp (datos, no van al repo) + reportes de mutation testing
creditos_*.xlsx
*.xlsx
/reports
.stryker-tmp/
reporte-*.html
# Exports de Disapp (PDF/planilla) — datos, no van al repo
*DISAPP*.pdf
Lista de Cr*.pdf

# Respaldos logicos de la base (DATOS DE CLIENTES — jamas a git)
backups/
respaldos-libro/
